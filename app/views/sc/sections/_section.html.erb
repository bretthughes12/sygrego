<tr>
    <td><%= section.session.name %></td>
    <td><%= section.name %></td>
    <td>
        <% unless section.results_locked %>
            <%= link_to "Results", sc_section_round_robin_matches_path(section_id: section.id),
                    class: primary_button_class,
                    role: "button" %>
        <% end %>
    </td>
</tr>
