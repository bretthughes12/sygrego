<%- headings = %w{Name CampPrefs } -%>
<%= headings.join(',') %>
<%= CSV.generate do |csv|
  @participants.each do |participant|
  	line = [ participant.name,
      			 participant.camping_preferences
		   ]
    csv << line
  end
end.html_safe %>
