<% title "Working With Children's Check for #{@participant.name}" %>

<h1><%= yield(:title) %></h1>

<%= render partial: "form_wwcc", locals: { 
                              url: update_wwcc_gc_participant_path(@participant), 
                              method: :patch } %>

<br/>

<%= link_to "Back", wwccs_gc_participants_path,
                  class: primary_button_class,
                  role: "button" %>
