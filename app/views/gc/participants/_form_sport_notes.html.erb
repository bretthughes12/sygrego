<%= simple_form_for(@participant, url: url, html: { method: method }) do |f| %> 
	<%= f.error_notification %>

	<%= f.input :lock_version, as: :hidden %>

	<% unless @sports_answers.empty? %>
		<%= render partial: 'questions', collection: @sports_answers, as: :answer, locals: {f: f} %>

		<br/>
    <% else %>
		<p>Your group has no sports questions for your participants.</p>
    <% end %>

	<%= f.button :submit %>
<% end %>	
