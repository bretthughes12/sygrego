<% title "SYG Registration for #{@group.short_name}" %>

<%= raw Seer::init_visualization -%>

<h1><%= yield(:title) %></h1>

Welcome! <br />
<br />

It is now <%= Time.now.in_time_zone.strftime("%d/%m/%Y %I:%M %p") %>.

<br/><br/>

<p><strong>Groups attending:</strong> <%= @total_groups %></p>
<p><strong>Participants attending:</strong> <%= @total_participants %></p>

<br/>

<%= render :partial => 'application/pie_chart', 
	:locals => { :chart_data => @participants_data,
				 :chart_element => "participants",
			     :chart_class => "chart",
				 :title => "Our participants (#{@participants_registered})" } %>
