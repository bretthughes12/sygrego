<% title "New fee category" %>

<h1><%= yield(:title) %></h1>

<%= render :partial => 'form', :locals => {:url => gc_group_fee_categories_path, :method => 'post'} %>

<br/>

<%= link_to "Back", gc_group_fee_categories_path,
                  class: primary_button_class,
                  role: "button" %>
