<% title "Volunteer vacancies" %>

<h1><%= yield(:title) %></h1>

<table class="table table-dark table-striped">
  <thead>
    <tr>
      <th>Type</th>
      <th>Description</th>
      <th>Session</th>
      <th>Venue</th>
    </tr>
  </thead>

  <%= render :partial => 'volunteer_vacant', :collection => @volunteers, :as => :volunteer %>
</table>

<%= link_to "Our Volunteers", gc_volunteers_path,
                  class: primary_button_class,
                  role: "button" %>
