<% title "Page details: #{@page.name}" %>

<h1><%= yield(:title) %></h1>

<%= render 'show', model: @page %>

<br/>

<strong>Content</strong>
<%= @page.content %>

<br/>

<%= link_to "Back", admin_pages_path,
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Edit", edit_admin_page_path(@page),
                  class: primary_button_class,
                  role: "button" %>
