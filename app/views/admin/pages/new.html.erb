<% title "New page" %>

<h1><%= yield(:title) %></h1>

<%= render partial: "form", locals: { 
                              url: admin_pages_path(@page), 
                              method: :post } %>

<br/>

<%= link_to "Back", admin_pages_path,
                  class: primary_button_class,
                  role: "button" %>
