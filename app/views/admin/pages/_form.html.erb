<%= simple_form_for(@page, url: url, html: { method: method }) do |f| %> 
	<%= f.error_notification %>

	<%= f.input :name, label: "Page" %>
	<%= f.input :admin_use %>
	<%= f.input :permalink %>
	<%= f.input :content, as: :rich_text_area %>

	<%= f.button :submit %>
<% end %>	
