<% title "Listing key dates" %>

<h1><%= yield(:title) %></h1>

<table class="table table-dark table-striped">
  <thead>
    <tr>
      <th>Date</th>
      <th>Title</th>
      <th>Description</th>
    </tr>
  </thead>

	<%= render @timelines %>
</table>

<%= link_to "New key date", new_admin_timeline_path,
                  class: primary_button_class,
                  role: "button" %>
