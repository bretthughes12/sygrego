<% title "New key date" %>

<h1><%= yield(:title) %></h1>

<%= render partial: "form", locals: { 
                              url: admin_timelines_path(@timeline), 
                              method: :post } %>

<br/>

<%= link_to "Back", admin_timelines_path,
                  class: primary_button_class,
                  role: "button" %>
