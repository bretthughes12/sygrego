<% title "Editing event details for #{@event_detail.group.name}" %>

<h1><%= yield(:title) %></h1>

<%= render partial: "form", locals: { 
                              url: admin_event_detail_path(@event_detail), 
                              method: :put } %>

<br/>

<%= link_to "Back", admin_event_details_path,
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Details", admin_event_detail_path(@event_detail),
                  class: primary_button_class,
                  role: "button" %>
