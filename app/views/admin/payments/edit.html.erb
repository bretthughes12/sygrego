<% title "Editing #{@payment.name}" %>

<h1><%= yield(:title) %></h1>

<%= render partial: "form", locals: { 
                              url: admin_payment_path(@payment), 
                              method: :patch } %>

<br/>

<%= link_to "Back", admin_payments_path,
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Details", admin_payment_path(@payment),
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Destroy", admin_payment_path(@payment),
                  method: :delete,
                  data: { confirm: "Are you sure?" },
                  class: danger_button_class,
                  role: "button" %>
