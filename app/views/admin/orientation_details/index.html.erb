<% title "Listing orientation details" %>

<h1><%= yield(:title) %></h1>

<table class="table table-dark table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Date / Time</th>
    </tr>
  </thead>

	<%= render @orientation_details %>
</table>

<br/>

<%= link_to "New orientation detail", new_admin_orientation_detail_path,
                  class: primary_button_class,
                  role: "button" %>
