<tr valign="top">
	<td> 
		<%= sport_coord.session_name %>
	</td>
    <td> 
    	<%= sport_coord.name %>
	</td>
    <td> 
    	<%= sport_coord.participant.name %>
		<% if sport_coord.collected %>
			<span class="badge bg-success">Collected</span>
		<% end %>
	</td>
</tr>
