<% title "Site Checklist for #{@rego_checklist.group.name}" %>

<h1><%= yield(:title) %></h1>

<%= render partial: "form_site_check", locals: { 
                              url: update_site_check_admin_rego_checklist_path(@rego_checklist), 
                              method: :patch } %>

<br/>

<%= link_to "Back", site_checks_admin_rego_checklists_path,
                  class: primary_button_class,
                  role: "button" %>
