<% title "SYG Registrations" %>

<h1><%= yield(:title) %></h1>

<%= search_box search_admin_rego_checklists_path, "Search" %>
<%= yield(:search_box)%>

<br/>

<table class="table table-dark table-striped">
  <thead>
    <tr>
      <th>Abbr</th>
      <th>Short Name</th>
      <th>Registered</th>
      <th>Mobile</th>
    </tr>
  </thead>

	<%= render @rego_checklists %>
</table>

<%= link_to "Download", admin_rego_checklists_path(format: :csv),
                  class: primary_button_class,
                  role: "button" %>