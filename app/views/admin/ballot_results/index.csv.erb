Sport,Grade,Section,Pref Section,Limit,Over Limit?,One Per Group?,Group,New Group?,Entry,Status,Factor
<%= CSV.generate do |csv|
  @ballot_results.each do |r|
    csv << 	[ 
				r.sport_name,
				r.grade_name,
				r.section_name,
                r.preferred_section_name,
				r.entry_limit,
				r.over_limit,
				r.one_entry_per_group,
				r.group_name,
				r.new_group,
				r.sport_entry_name,
				r.sport_entry_status,
				r.factor
	 		]
  end
end.html_safe %>