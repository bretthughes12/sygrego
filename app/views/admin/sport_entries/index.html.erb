<% title "Listing sport entries" %>

<h1><%= yield(:title) %></h1>

<%= will_paginate @sport_entries, :container => false %>

<%= link_to "New sport entry", new_admin_sport_entry_path,
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Download", admin_sport_entries_path(format: :xlsx),
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Upload", new_import_admin_sport_entries_path,
                  class: danger_button_class,
                  role: "button" %>

<br/>
<br/>

<table class="table table-dark">
  <thead>
    <tr>
      <th>Session</th>
      <th>Grade</th>
      <th>Group</th>
      <th>Name</th>
      <th>Venue</th>
    </tr>
  </thead>

	<%= render @sport_entries %>
</table>

<%= will_paginate @sport_entries, :container => false %>

<%= link_to "New sport entry", new_admin_sport_entry_path,
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Download", admin_sport_entries_path(format: :xlsx),
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Upload", new_import_admin_sport_entries_path,
                  class: danger_button_class,
                  role: "button" %>

<%= legend_box [{:name => "Entered", :class => "table-primary"},
					{:name => "Requested", :class => "table-warning"},
					{:name => "Requiring Confirmation", :class => "table-danger"},
					{:name => "Waiting List", :class => "table-dark"}] %>
