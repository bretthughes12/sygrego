<% title "Editing #{@voucher.name}" %>

<h1><%= yield(:title) %></h1>

<%= render partial: "form", locals: { 
                              url: admin_voucher_path(@voucher), 
                              method: :patch } %>

<br/>

<%= link_to "Back", admin_vouchers_path,
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Details", admin_voucher_path(@voucher),
                  class: primary_button_class,
                  role: "button" %>
<%= link_to "Destroy", admin_voucher_path(@voucher),
                  method: :delete,
                  data: { confirm: "Are you sure?" },
                  class: danger_button_class,
                  role: "button" %>
