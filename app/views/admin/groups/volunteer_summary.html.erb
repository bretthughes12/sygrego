<% title "Group Volunteer Summary" %>

<h1><%= yield(:title) %></h1>

<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th>Church</th>
            <th>Abbr</th>
            <th>Participants</th>
            <th>Required</th>
            <th>Volunteers</th>
        </tr>
    </thead>

    <%= render :partial => 'group_volunteer_summary', :collection => @groups, :as => :group %>
</table>
