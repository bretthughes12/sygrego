<% title "Editing #{@group.name}" %>

<h1><%= yield(:title) %></h1>

<%= render partial: "form_approval", locals: { 
                              url: update_approval_admin_group_path(@group), 
                              method: :patch } %>

<br/>

<%= link_to "Back", approvals_admin_groups_path,
                  class: primary_button_class,
                  role: "button" %>
