<%- headings = %w{RowID Active Name} -%>
<%= headings.join(',') %>
<%= CSV.generate do |csv|
  @sessions.each do |session|
	line =  [ session.database_rowid, 
            session.active,
		        session.name ]
    csv << line 
  end
end.html_safe %>
