Day,Session,Church,Participants
<%= CSV.generate do |csv|
  @sat_early.each do |g|
    csv << 	[ 
				"Saturday",
				"Early",
				g.name,
				g.participants.coming.accepted.size
	 		]
  end

  @sat_late.each do |g|
    csv << 	[ 
				"Saturday",
				"Late",
				g.name,
				g.participants.coming.accepted.size
	 		]
  end

  @sat_no_pref.each do |g|
    csv << 	[ 
				"Saturday",
				"No preference",
				g.name,
				g.participants.coming.accepted.size
	 		]
  end

  @sun_early.each do |g|
    csv << 	[ 
				"Sunday",
				"Early",
				g.name,
				g.participants.coming.accepted.size
	 		]
  end

  @sun_late.each do |g|
    csv << 	[ 
				"Sunday",
				"Late",
				g.name,
				g.participants.coming.accepted.size
	 		]
  end

  @sun_no_pref.each do |g|
    csv << 	[ 
				"Sunday",
				"No preference",
				g.name,
				g.participants.coming.accepted.size
	 		]
  end
end.html_safe %>