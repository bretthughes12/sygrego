<% title "New Warden Zone" %>

<h1><%= yield(:title) %></h1>

<%= render partial: "form", locals: { 
                              url: admin_warden_zones_path(@warden_zone), 
                              method: :post } %>

<br/>

<%= link_to "Back", admin_warden_zones_path,
                  class: primary_button_class,
                  role: "button" %>
